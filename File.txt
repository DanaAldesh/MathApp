NavigationLink(
    destination: VideoLessonsView(
        videos: [
            "natural_numbers",
            "percent",
            "decimal"
        ]
    ),
    isActive: $appState.showAllConspects
) { EmptyView() }
.hidden()
